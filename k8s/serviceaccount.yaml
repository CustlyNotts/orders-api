# One identity per app, never the namespace's "default" account.
# orders-api never calls the Kubernetes API, so no token is mounted and
# there is no Role. See examples/role-read-config.yaml for when an app does.
apiVersion: v1
kind: ServiceAccount
metadata:
  name: orders-api
  namespace: orders
automountServiceAccountToken: false
