{
  "name": "orders-api",
  "version": "1.0.0",
  "description": "Demo service for the Security by Design talk: one commit, hardened from laptop to Kubernetes.",
  "private": true,
  "license": "MIT",
  "main": "src/server.js",
  "engines": {
    "node": ">=24"
  },
  "scripts": {
    "start": "node src/server.js",
    "test": "node --test"
  }
}
